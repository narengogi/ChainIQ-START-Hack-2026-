// This script builds the knowledge graph in neo4j for the procurement pipeline based on the previous awards.

/*
nodes and edges:
- node: supplier (id)
- edge: 
*/

import { getDriver } from "../src/db/neo4j";

// const driver = getDriver();

// const session = driver.session({ database: "chainiq" });

// const result = await session.run("MATCH (n) RETURN n");

// console.log(result.records);

// session.close();


async function main() {
    
}